# Performance

No Telegram end-to-end latency baseline is verified in this repository. `make test` runs race-enabled unit tests; `make build` verifies both local binaries. Add measured, reproducible latency or memory data here only after recording the exact host, command, and run count.
