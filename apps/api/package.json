{
  "name": "@kelpclaw/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/server.js",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.test.json --noEmit"
  },
  "dependencies": {
    "@kelpclaw/codegen": "workspace:*",
    "@kelpclaw/nanoclaw": "workspace:*",
    "@kelpclaw/skill-registry": "workspace:*",
    "@kelpclaw/workflow-spec": "workspace:*",
    "fastify": "^5.6.2"
  }
}
