import type { TaintLedgerEntry } from "../types/taint.js";

// TODO: phase 2C extract tainted spans from case artifacts and Protocol SIFT output.
export function extractTaintSpans(file: string): readonly TaintLedgerEntry[] {
  void file;
  return [];
}
