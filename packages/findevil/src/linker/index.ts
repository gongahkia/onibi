import type { Claim } from "../types/claim.js";

// TODO: phase 2A link claims to timeline, Prefetch, Amcache, Sysmon, and tool-output evidence.
export function linkEvidence(claim: Claim, caseDir: string): Claim {
  void caseDir;
  return claim;
}
