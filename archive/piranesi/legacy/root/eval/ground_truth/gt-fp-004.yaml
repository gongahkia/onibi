id: gt-fp-004
source_project: synthetic
commit_hash: "synthetic-no-commit"
cwe_id: CWE-89
cwe_name: SQL Injection
label: false_positive
affected_files:
  - eval/synthetic/fp-prisma-typesafe.ts
line_numbers: [12, 16]
taint_source: req.body.email
taint_sink: "prisma.user.findMany() with where clause"
taint_path:
  - "req.body.email (Express route handler)"
  - "const users = await prisma.user.findMany({ where: { email: req.body.email } })"
complexity: simple
exploitable: false
reference_exploit: null
reference_fix_commit: null
notes: "Hand-crafted false positive. Prisma ORM type-safe query API. User input reaches the database query, but Prisma's query builder generates parameterized SQL internally. No raw query or string interpolation involved. Pattern superficially resembles injection to naive analyzers that flag any user input reaching a DB call."
language: typescript
framework: general
taint_step_count: 2
taint_field_path: body.email
field_sensitive_label: false_positive
