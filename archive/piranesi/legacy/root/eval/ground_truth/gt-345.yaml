id: gt-345
source_project: synthetic-phase30
commit_hash: synthetic-no-commit
cwe_id: CWE-918
cwe_name: Server-Side Request Forgery
label: true_positive
affected_files:
- eval/fixtures/python/fastapi/gt-345/main.py
line_numbers:
- 11
taint_source: 'url: str = Query(...) in dependency get_target()'
taint_sink: requests.get()
taint_path:
- FastAPI binds query data into the dependency parameter url
- Depends(get_target) forwards the normalized but still untrusted URL
- proxy() calls requests.get() with the dependency result
complexity: inter
exploitable: true
reference_exploit: GET /proxy?url=http://169.254.169.254/latest/meta-data/ triggers
  SSRF through the dependency output.
reference_fix_commit: null
notes: FastAPI dependency injection misuse where a dependency returns an attacker-chosen
  URL.
discovery_method: synthetic
language: python
framework: fastapi
taint_step_count: 3
taint_field_path: query.url
field_sensitive_label: true_positive
