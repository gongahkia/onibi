apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: arachne-relay
  labels:
    app.kubernetes.io/component: relay
    app.kubernetes.io/name: arachne
spec:
  replicas: 1
  podManagementPolicy: OrderedReady
  serviceName: arachne-relay-headless
  updateStrategy:
    type: OnDelete
  selector:
    matchLabels:
      app.kubernetes.io/component: relay
      app.kubernetes.io/name: arachne
  template:
    metadata:
      labels:
        app.kubernetes.io/component: relay
        app.kubernetes.io/name: arachne
    spec:
      automountServiceAccountToken: false
      serviceAccountName: arachne-relay
      terminationGracePeriodSeconds: 35
      securityContext:
        fsGroup: 65532
        fsGroupChangePolicy: OnRootMismatch
        runAsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: relay
          image: ghcr.io/gongahkia/arachne-relay:0.1.0
          imagePullPolicy: IfNotPresent
          args:
            - run
            - --config
            - /etc/arachne/relay.conf
            - --identity
            - /run/secrets/relay.identity
            - --health-address
            - 127.0.0.1:8080
            - --metrics-address
            - 127.0.0.1:8081
            - --graceful-shutdown-timeout-seconds
            - "30"
          ports:
            - containerPort: 50051
              name: grpc
              protocol: TCP
          resources:
            limits:
              cpu: "1"
              memory: 512Mi
            requests:
              cpu: 250m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
          startupProbe:
            exec:
              command:
                - /usr/local/bin/arachne-relay
                - healthcheck
                - --address
                - 127.0.0.1:8080
            failureThreshold: 30
            periodSeconds: 2
            timeoutSeconds: 2
          livenessProbe:
            exec:
              command:
                - /usr/local/bin/arachne-relay
                - healthcheck
                - --address
                - 127.0.0.1:8080
            failureThreshold: 3
            periodSeconds: 10
            timeoutSeconds: 2
          readinessProbe:
            exec:
              command:
                - /usr/local/bin/arachne-relay
                - healthcheck
                - --address
                - 127.0.0.1:8080
            failureThreshold: 3
            periodSeconds: 5
            timeoutSeconds: 2
          volumeMounts:
            - mountPath: /etc/arachne
              name: config
              readOnly: true
            - mountPath: /run/secrets
              name: relay-identity
              readOnly: true
            - mountPath: /var/lib/arachne
              name: data
      volumes:
        - configMap:
            name: arachne-relay-config
          name: config
        - name: relay-identity
          secret:
            defaultMode: 288
            items:
              - key: relay.identity
                path: relay.identity
            secretName: arachne-relay-identity
  volumeClaimTemplates:
    - metadata:
        labels:
          app.kubernetes.io/component: relay
          app.kubernetes.io/name: arachne
        name: data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 10Gi
