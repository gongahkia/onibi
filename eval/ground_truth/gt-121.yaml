id: gt-121
source_project: piranesi-owasp-fixture
commit_hash: "0000000000000000000000000000000000000000"
cwe_id: CWE-502
cwe_name: Unsafe Deserialization (pickle.loads from user input)
label: true_positive
affected_files:
  - deserialization.py
line_numbers: [12, 13]
taint_source: "request.get_data()"
taint_sink: "pickle.loads(data)"
taint_path:
  - "request.get_data() (deserialization.py:12)"
  - "pickle.loads(data) (deserialization.py:13)"
complexity: simple
exploitable: true
reference_exploit: "POST /api/load with crafted pickle payload for arbitrary code execution via __reduce__"
reference_fix_commit: null
notes: "pickle.loads on untrusted input is critical severity — allows arbitrary code execution via __reduce__ method."
language: python
framework: general
taint_step_count: 2
taint_field_path: body.raw
field_sensitive_label: true_positive
