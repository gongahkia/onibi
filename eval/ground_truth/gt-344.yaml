id: gt-344
source_project: synthetic-phase30
commit_hash: synthetic-no-commit
cwe_id: CWE-89
cwe_name: SQL Injection
label: true_positive
affected_files:
- eval/fixtures/python/fastapi/gt-344/main.py
line_numbers:
- 13
taint_source: 'order_by: str = Query("id") in dependency get_order()'
taint_sink: cursor.execute()
taint_path:
- FastAPI binds query data into the dependency parameter order_by
- Depends(get_order) forwards the untrusted value into the route handler
- audit() appends order_clause onto SQL text
- cursor.execute() evaluates the attacker-controlled ORDER BY fragment
complexity: inter
exploitable: true
reference_exploit: GET /audit?order_by=id DESC, (SELECT sqlite_version()) injects
  SQL through the dependency result.
reference_fix_commit: null
notes: FastAPI dependency injection misuse where a dependency returns raw SQL syntax.
discovery_method: synthetic
language: python
framework: fastapi
taint_step_count: 4
