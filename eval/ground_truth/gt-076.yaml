id: gt-076
source_project: owasp-juice-shop
commit_hash: "7380ce7120e289fc6bea861efd3fcba89261a6a8"
cwe_id: CWE-22
cwe_name: Path Traversal
label: false_positive
affected_files:
  - routes/profileImageFileUpload.ts
line_numbers: [24, 30, 41, 43]
taint_source: req.file.buffer
taint_sink: "fs.writeFile() with fixed server-side destination path"
taint_path:
  - "req.file.buffer (routes/profileImageFileUpload.ts:17-18)"
  - "fileType.fromBuffer(buffer) determines the extension from file contents (routes/profileImageFileUpload.ts:24)"
  - "const filePath = `frontend/dist/frontend/assets/public/images/uploads/${loggedInUser.data.id}.${uploadedFileType.ext}` uses only server-derived path components (routes/profileImageFileUpload.ts:41)"
  - "fs.writeFile(filePath, buffer) writes to that fixed location (routes/profileImageFileUpload.ts:43)"
complexity: simple
exploitable: false
reference_exploit: null
reference_fix_commit: null
notes: "False positive for path traversal. User input controls the file contents, but not the path. The destination is derived from the authenticated user id and detected image type."
language: typescript
framework: express
taint_step_count: 4
taint_field_path: files.file.buffer
field_sensitive_label: false_positive
