id: gt-060
source_project: owasp-juice-shop
commit_hash: "7380ce7120e289fc6bea861efd3fcba89261a6a8"
cwe_id: CWE-94
cwe_name: Code Injection (Server-Side Template Injection)
label: true_positive
affected_files:
  - routes/updateUserProfile.ts
  - routes/userProfile.ts
line_numbers: [36, 55, 57, 62, 98]
taint_source: req.body.username
taint_sink: "eval() on username-derived code during profile rendering"
taint_path:
  - "req.body.username is stored through user.update({ username: req.body.username }) (routes/updateUserProfile.ts:36)"
  - "GET /profile reloads user.username from the database (routes/userProfile.ts:42-53)"
  - "username?.match(/#{(.*)}/) recognizes template-style payloads (routes/userProfile.ts:55)"
  - "const code = username.substring(2, username.length - 1) extracts attacker-controlled JavaScript (routes/userProfile.ts:57)"
  - "username = eval(code) executes it server-side (routes/userProfile.ts:62)"
  - "res.send(fn(user)) returns the page after the injected code has already run (routes/userProfile.ts:98)"
complexity: ctx
exploitable: true
reference_exploit: "POST /profile with username=#{global.process.version} proves server-side evaluation; replacing the expression with a child_process payload yields full code execution when /profile is rendered."
reference_fix_commit: null
notes: "Legacy profile rendering performs eval() on a persisted username pattern. This is the server-side template/code-injection half of Juice Shop's SSTI challenge path."
language: typescript
framework: express
taint_step_count: 6
taint_field_path: body.username
field_sensitive_label: true_positive
