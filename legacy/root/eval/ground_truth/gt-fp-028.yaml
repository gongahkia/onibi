id: gt-fp-028
source_project: synthetic
commit_hash: "synthetic-no-commit"
cwe_id: CWE-89
cwe_name: SQL Injection
label: false_positive
affected_files:
  - eval/synthetic/fp-allowlist-order-by.ts
line_numbers: [15]
taint_source: req.query.order
taint_sink: "client.query() behind ORDER BY allowlist"
taint_path:
  - "req.query.order (Express GET handler)"
  - "if (!allowed.includes(order)) return 400"
  - "client.query(`SELECT * FROM users ORDER BY ${order}`)"
complexity: simple
exploitable: false
reference_exploit: null
reference_fix_commit: null
notes: "Hand-crafted false positive. The allowlist constrains ORDER BY to three known column names, so arbitrary SQL syntax cannot reach the sink."
language: typescript
framework: general
taint_step_count: 3
taint_field_path: query.order
field_sensitive_label: false_positive
