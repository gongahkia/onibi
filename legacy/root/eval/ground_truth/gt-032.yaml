id: gt-032
source_project: synthetic
commit_hash: "synthetic-no-commit"
cwe_id: CWE-89
cwe_name: SQL Injection
label: true_positive
affected_files:
  - eval/synthetic/sqli-pg-raw.ts
line_numbers: [12, 15]
taint_source: req.params.id
taint_sink: "pg client.query() with string concatenation"
taint_path:
  - "req.params.id (Express GET handler)"
  - "const result = await client.query('SELECT * FROM orders WHERE id = ' + req.params.id)"
  - "pg client.query() with concatenated user input"
complexity: simple
exploitable: true
reference_exploit: "GET /orders/1 UNION SELECT username,password FROM users-- exfiltrates credentials"
reference_fix_commit: null
notes: "Hand-crafted synthetic. Raw pg client with string concatenation instead of parameterized query. Fix: use client.query('SELECT ... WHERE id = $1', [id])."
language: typescript
framework: general
taint_step_count: 3
taint_field_path: params.id
field_sensitive_label: true_positive
