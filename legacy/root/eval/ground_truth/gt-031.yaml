id: gt-031
source_project: synthetic
commit_hash: "synthetic-no-commit"
cwe_id: CWE-89
cwe_name: SQL Injection
label: true_positive
affected_files:
  - eval/synthetic/sqli-knex-raw.ts
line_numbers: [11, 14]
taint_source: req.query.sort
taint_sink: "knex.raw()"
taint_path:
  - "req.query.sort (Express GET handler)"
  - "const results = await knex.raw(`SELECT * FROM items ORDER BY ${req.query.sort}`)"
  - "knex.raw() with template literal interpolation bypasses parameterization"
complexity: simple
exploitable: true
reference_exploit: "GET /items?sort=1;DROP TABLE items-- injects arbitrary SQL via ORDER BY clause"
reference_fix_commit: null
notes: "Hand-crafted synthetic. knex.raw() with template literal. Developers often forget to parameterize ORDER BY/LIMIT clauses even when using query builders."
language: typescript
framework: general
taint_step_count: 3
taint_field_path: query.sort
field_sensitive_label: true_positive
