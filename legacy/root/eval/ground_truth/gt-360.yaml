id: gt-360
source_project: synthetic-phase30-go
commit_hash: synthetic-no-commit
cwe_id: CWE-79
cwe_name: Cross-Site Scripting
label: true_positive
affected_files:
- eval/fixtures/go/gin/gt-360/main.go
line_numbers:
- 9
taint_source: c.Query("name")
taint_sink: c.Writer.Write()
taint_path:
- c.Query("name") reads attacker-controlled HTML content.
- The handler embeds the value inside a raw HTML fragment.
- c.Writer.Write([]byte(...)) sends the unescaped markup to the browser.
complexity: simple
exploitable: true
reference_exploit: GET /banner?name=<img src=x onerror=alert(1)>
reference_fix_commit: null
notes: Gin string-concatenates user data into HTML before writing it.
discovery_method: synthetic
language: go
framework: gin
taint_step_count: 3
taint_field_path: query.name
field_sensitive_label: true_positive
