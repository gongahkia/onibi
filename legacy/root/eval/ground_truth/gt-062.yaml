id: gt-062
source_project: owasp-juice-shop
commit_hash: "7380ce7120e289fc6bea861efd3fcba89261a6a8"
cwe_id: CWE-22
cwe_name: Path Traversal
label: true_positive
affected_files:
  - routes/dataErasure.ts
line_numbers: [68, 69, 72, 79]
taint_source: req.body.layout
taint_sink: "res.render() with attacker-controlled Express-Handlebars layout option"
taint_path:
  - "req.body.layout (routes/dataErasure.ts:68)"
  - "path.resolve(req.body.layout).toLowerCase() only blocks a few forbidden substrings (routes/dataErasure.ts:69-70)"
  - "res.render('dataErasureResult', { ...req.body }, callback) forwards layout into the render options object (routes/dataErasure.ts:72)"
  - "Express-Handlebars treats layout as a filesystem path and reads the attacker-selected file"
  - "the callback truncates and returns the leaked file content to the client (routes/dataErasure.ts:78-79)"
complexity: ctx
exploitable: true
reference_exploit: "POST /dataerasure with body layout=../package.json leaks the beginning of package.json and appends '......', as covered by the upstream API test."
reference_fix_commit: null
notes: "Local file read via attacker-controlled Handlebars layout resolution. The view name stays fixed, but the spread render options make layout a file-system sink."
language: typescript
framework: express
taint_step_count: 5
taint_field_path: body.layout
field_sensitive_label: true_positive
