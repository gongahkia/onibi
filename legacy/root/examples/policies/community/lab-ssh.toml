[profile]
name = "community-lab-ssh"
description = "Community starter policy for lab SSH posture review."

[[required_evidence]]
key = "listening_ports"
severity = "warn"

[[required_evidence]]
key = "users"
severity = "warn"

[[gate]]
id = "no-critical-or-high"
description = "Lab hosts should not carry critical or high active findings."
max_severity = "medium"
action = "fail"
