# What's Next

- Add live Protocol SIFT parsers for the CFReDS E01 so worksheet prompts can be confirmed from recovered email headers, VHD metadata, SAM records, recycle-bin records, browser history, autoruns, and direct file hashes.
- Expand direct memory and process-access artifacts for credential-access claims so T1003 findings can move beyond inferred status.
- Add remote-logon and destination-host fixtures for lateral-movement claims so T1021 can be confirmed or contradicted instead of unverifiable.
- Broaden DFIR-Metric coverage beyond subset-10 while preserving the deterministic offline trace path for judging and CI.
- Add more Sigma rule families and export richer ATT&CK Navigator scoring comments.
- Keep signed reviewer HTML, raw JSON/JSONL, RFC3161 timestamp tokens, and deterministic replay hashes in lockstep so the human-friendly view never replaces the audit source of truth.
